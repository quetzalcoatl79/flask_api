export default function Footer() {
  return (
    <footer className="w-full mt-12 py-6 text-center text-xs text-gray-500 border-t">
      <p>Starter Flask + Next.js + Tailwind + Celery • {new Date().getFullYear()}</p>
    </footer>
  );
}
